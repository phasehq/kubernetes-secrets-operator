// Package version holds the operator's hard-coded version.
package version

// Version is the Phase Kubernetes Operator version. Keep this in sync with the
// Helm chart appVersion in phase-kubernetes-operator/Chart.yaml.
const Version = "2.0.0"
